name: CI/CD for React + Node.js/Express

run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:

  build-and-test:
    runs-on: ubuntu-latest

    steps:
      # Checkout code
      - name: Checkout repository
        uses: actions/checkout@v3
      
      # Setup Node.js
      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'
      
            # Install dependencies for backend
      - name: Install backend dependencies
        working-directory: ./
        run: npm install

      # Run backend tests
      - name: Run backend PM for testing
        working-directory: ./
        run: npm run prod:server

      # Run backend tests
      - name: Run backend tests
        working-directory: ./packages/server
        run: npm test

      
      # Install dependencies for frontend
      - name: Install frontend dependencies
        working-directory: ./packages/frontend
        run: npm install

      # Run frontend tests
      - name: Run frontend tests
        working-directory: ./packages/frontend
        run: npm test

      # Build frontend
      - name: Build frontend
        working-directory: ./packages/frontend
        run: npm run build

      # (Optional) Upload build artifacts (React build)
      - name: Upload production-ready frontend
        uses: actions/upload-artifact@v4
        with:
          name: react-build
          path: ./packages/frontend/build

  deploy:
    needs: build-and-test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      # Deploy via SSH to a VPS
      # - name: Deploy to server
      #   uses: appleboy/ssh-action@v0.1.10
      #   with:
      #     host: ${{ secrets.SERVER_HOST }}
      #     username: ${{ secrets.SERVER_USER }}
      #     key: ${{ secrets.SERVER_SSH_KEY }}
      #     script: |
      #       cd /var/www/your-app
      #       git pull origin main
      #       npm install --prefix server
      #       npm install --prefix client
      #       npm run build --prefix client
      #       pm2 restart all
